# This a project to use test and learn full-stack development. Following roadmap.sh website's full-stack roadmap.

## Initialize the folder as a git repository
*git init*

## Initialize the project with an npm package
*npm init y*